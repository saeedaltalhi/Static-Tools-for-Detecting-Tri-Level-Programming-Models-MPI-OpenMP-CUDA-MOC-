#include<omp.h>
#include<stdlib.h>

int main(void)
{
#	pragma omp parallel
	{
#		pragma omp for
		for (int i = 0; i < 10; i++)
		{
			// Do some work
#			pragma omp for
			for (int j = 0; j < 10; j++)
			{
				// Do some work
			}
		}
	}	
	return 0;
}